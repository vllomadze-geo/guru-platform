# GURU Platform v1.1.8

Исправление инструмента «Экспорт диагностики».

## Что изменено

- Убран экспорт диагностики из каждого отдельного блока.
- Оставлена одна кнопка в панели Gate: «Экспорт диагностики Gate».
- Кнопка работает на любом Gate и экспортирует весь текущий Gate.
- Исправлена ошибка, из-за которой файл не скачивался.
- Экспорт содержит: Gate, блоки, поля, статусы, доказательства, глобальные данные, конфликты, ошибки валидации, UI-состояние, console errors.
- Supabase, Яндекс и структура Gate 0 сохранены.

## Commit message

Update to v1.1.8: fix Gate debug export


## v1.1.9 — Gate Debug Export hard fix

- Убран prompt перед экспортом, кнопка сразу формирует debug export.
- Экспорт работает на уровне всего активного Gate, без кнопок в отдельных блоках.
- Добавлен видимый fallback-панель: если браузер не скачал файл автоматически, можно нажать `Скачать JSON` или `Скопировать`.
- JSON собирается безопасно, с защитой от циклических объектов и слишком длинных строк.
- Supabase, Яндекс и Gate 1–7 не пересобирались.
